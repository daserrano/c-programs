#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]){

    const char *list[] = {"yield", "yelp", "yellow", "constantino"};

    numero_palabras = (sizeof(list) / sizeof(char *)); // Numero de palabras que hay en lista.

    for(int posicion=0; posicion < numero_palabras; posicion ++)
	for(int siguiente_palabra=posicion+1; siguiente_palabra < numero_palabras; siguiente_palabra++){
	    comparar = (strcmp(list[posicion], list[siguiente_palabra]);
      





	}



    return EXIT_SUCCESS;
}
