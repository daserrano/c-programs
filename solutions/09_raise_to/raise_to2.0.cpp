/* Write a program that the user give a number and an exponent. */

#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[]){


if(argc < 3)
    return EXIT_FAILURE;

double base = atof(argv[1]);

int exponent = atoi(argv[2]);









    return EXIT_SUCCESS;
}
